  let arr = [4, 5, 6, 7, 0, 3, 2, 8, 9, 10];

  function abc(arr) {
    let val = [];
    let large = 0;
    arr.forEach((ele) => {
      if (large <= ele) {
        large = ele;
      } else {
        val.push(ele);
      }
    });
    return val;
  }

  console.log(abc(arr));
