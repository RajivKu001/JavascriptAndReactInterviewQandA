
  const person = {
    name: "John",
    age: 30
  };

  const frozenPerson = Object.freeze(person);

  frozenPerson.age = 40; // Attempted modification

  console.log(frozenPerson.age); // Output: 30

    //  <h1>immutableObjects</h1>
