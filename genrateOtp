  const arr = [1, 2, 3, 4, 5];
  const arr1 = [3, 4, 5, 6, 7];

  function genrateOtp(arr1, arr2) {
    let otp = '';
    for (let i = 0; i < 6; i++) {
      otp = otp + Math.floor(Math.random() * 10);
    }

    return otp;
  }
  console.log(genrateOtp(arr, arr1));
