  const strArr = ["hello", "India", "hello", "hello"];

  function myFun(arr) {
    let count = {};
    arr.forEach((ele) => {
      if (count[ele]) {
        count[ele]++;
      } else {
        count[ele] = 1;
      }
    });
    return count;
  }
  console.log(myFun(strArr));

